<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:colorBackground">@android:color/white</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ExtremelyEasy" parent="Theme.AppCompat.NoActionBar" >
        <item name="android:colorBackground">@color/background_material_light</item>
        <item name="android:textColorPrimary">@android:color/holo_blue_dark</item>
        <item name="android:textColorSecondary">@android:color/holo_green_light</item>
        <item name="android:textColorSecondaryInverse">@android:color/holo_orange_dark</item>
        <item name="android:textColorPrimaryInverse">@android:color/holo_orange_dark</item>
        <item name="android:colorForeground">@android:color/holo_purple</item>
    </style>

    <style name="Holo_Dark" parent="android:Theme.Holo.NoActionBar" />

    <style name="Holo_Light" parent="android:Theme.Holo.Light.NoActionBar" />

    <style name="Nicky" parent="Theme.AppCompat.Light.NoActionBar" >
        <item name="android:colorBackground">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/duplicate_color</item>
    </style>

</resources>
